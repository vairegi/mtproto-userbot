GUIDE_APPEND — v12.80 (2026-09-07) — Bot 2 cache-blindness fix
WHAT: Bot 2 downloaded nothing from Bot 1's overnight scrape (1200+ items).
Every cycle: "mongo2 list_gallery_ids failed: '<' not supported between
instances of 'str' and 'int'" — Mongo-2 cached_at is mixed str/int, the
Python fallback sort crashed, and the function returned [] every cycle
(dashboard: "Lifetime cached (Turso): 0"). Fix: cached_at is coerced with
float() (garbage -> 0.0) in BOTH turso_store fallback sorts AND the
producer's gallery_rows sort (same landmine, would have crashed next).
FILES: Bot2Fetcher/app/turso_store.py, Bot2Fetcher/app/fetcher.py,
GUIDE.md, GUIDE_APPEND.txt.
DEPLOY: drop on repo root, commit, push, redeploy Bot 2 ONLY.
VERIFY: first cycle after deploy logs "📖 [MONGO READ] list_gallery_ids —
N rows (BOT2_TURSO_OFF)" with N in the thousands (was absent/crashing);
"📡 scan cycle N: M candidate galleries queued" with large M; dashboard
"Lifetime cached" climbs from 0; slots start claiming the backlog. The
warning line disappears.
