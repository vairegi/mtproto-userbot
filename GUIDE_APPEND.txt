GUIDE_APPEND — v12.87 (2026-09-08) — delivery watcher (auto-DM on Bot 2 completion)
WHAT: since worker.py's removal (v12.66) nothing delivered a Bot-2-fetched
PDF to the requesting mini-app user — they had to tap Download twice. Bot 0
now runs delivery_watcher: a 10s background loop that picks queue rows
flipped to 'completed' (boot watermark = boot-1h, so no retro-spam), waits
for the galleries doc to carry both db_cover_msg_id + db_pdf_msg_id, then
DMs cover+PDF via the existing dm_delivery pipeline (force-join, BackupDB
toggle, share-guard, auto-delete all apply). Ledger gets delivered=True /
"force_join" / "skipped" / "failed" stamps; transient errors retry for up
to 6h. Completion toast now reads "📨 Check your DM".
FILES: miniapp/backend/app/services/delivery_watcher.py (NEW),
miniapp/backend/main.py, miniapp/frontend/js/plugins/card-actions.js,
GUIDE.md, GUIDE_APPEND.txt.
DEPLOY: drop on repo root, commit, push, redeploy Bot 0 ONLY. No new env
vars required (rollback switch: DELIVERY_WATCHER_OFF=1).
VERIFY: Render logs show "delivery_watcher background loop started"; after
a user-queued download finishes, "📨 delivery_watcher: #<gid> auto-DM'd to
user <uid>" and the PDF lands in the user's DM without a second tap.
