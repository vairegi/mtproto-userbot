GUIDE_APPEND — v12.81 (2026-09-07) — 5h log digest + repost-spam fix
WHAT: (1) NEW log-channel message every 5h (LOG_SUMMARY_HOURS env,
default 5) with window totals: fetched/failed/dropped/floodwaits/claimed/
cycles + per-slot breakdown + lifetime done; window resets per digest.
(2) Live status now edits every 20s (was 30s). (3) Fixed new-message spam:
failed edits retry next tick; a fresh live-status message is only sent
after 10min of continuous edit failures, max one per 10min.
FILES: Bot2Fetcher/app/dashboard.py, Bot2Fetcher/app/config.py, GUIDE.md,
GUIDE_APPEND.txt.
DEPLOY: drop on repo root, commit, push, redeploy Bot 2 ONLY.
VERIFY: Render log every 20s shows edit traffic (no new sendMessage
storms); at boot+5h the log channel receives "BOT 2 — 5H DIGEST" with
window counts matching the slots' completed jobs. To test sooner, set
LOG_SUMMARY_HOURS=0 (0h) is NOT supported — use a small value like 1 for a
quick check, then set back to 5.
