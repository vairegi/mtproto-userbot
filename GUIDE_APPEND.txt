GUIDE_APPEND — v12.83 (2026-09-07) — log recovery + RAM visibility
WHAT: (1) If the live status message is deleted from the log channel
(prod: edits 400ed "message to edit not found" from 05:10 on), the
dashboard now reposts on the next ~20s tick instead of waiting out the
10min anti-spam path; generic edit failures unchanged. (2) RAM everywhere:
live message footer "RAM: NNNMB / 512MB (NN%)", same line in the 5h
digest, "🧠 boot RAM" line in Render logs, and a new /checkram command in
the log channel (polled once per 20s tick via getUpdates timeout=0).
psutil added to Bot2Fetcher/requirements.txt; missing psutil degrades to
"RAM: n/a", never crashes.
FILES: Bot2Fetcher/app/dashboard.py, Bot2Fetcher/main.py,
Bot2Fetcher/requirements.txt, GUIDE.md, GUIDE_APPEND.txt.
DEPLOY: drop on repo root, commit, push, redeploy Bot 2 ONLY (Render
installs psutil from requirements automatically).
VERIFY: Render boot log shows "🧠 boot RAM: NNN MB RSS"; live status
message footer shows the RAM line and refreshes every 20s; send /checkram
in the log channel -> bot replies with current RSS; delete the live
status message -> a fresh one appears within ~20s.
