================================================================================
GUIDE_APPEND — v12.57 (2026-09-04) — Turso read-budget emergency brake
================================================================================

CONTEXT
-------
Reads jumped 293M -> 300.05M in 70 minutes (~100k rows/min, ~144M/day pace
-> budget exhausted in ~2 days). Live code + census showed the burners were
NOT the empty tables (bot2_done, bot2_queue, bm_cover, cache, bucket_replica
all hold 0 rows — moving them to Mongo would save nothing). The real
burners: Bot 2's gallery:* scans (15k rows touched per scan even in "delta"
mode before the cached_at index), /similar's full-table json_each scan
(15k rows PER USER REQUEST), and Bot 0's unbounded dedup gallery walk.
Bot 2 was manually suspended by Ryan as an immediate brake; this zip makes
its scans safe to re-enable.

CHANGES
-------
1) miniapp/backend/app/routes/suggestions.py — /similar KILL SWITCH.
   gallery_suggestions() now returns {"ok": false, "error":
   "similar-disabled", "items": [], "results": []} immediately unless
   SIMILAR_ENABLED=1 is set on the Bot 0 Render service. Default OFF.
   (User-requested temporary disable until budget recovers; the toggle is
   env-only, redeploy-free.)

2) Bot2Fetcher/app/turso_store.py — scan hardening:
     BOT2_LIST_MEMO_TTL_SEC        default 10  -> 120  (one scan / 2 min max)
     BOT2_LIST_FULL_RESCAN_EVERY   default 12  -> 144  (full 15k-row sweep
                                                        roughly daily, not hourly)
     BOT2_LIST_WATERMARK_SLACK_S   default 60  -> 300  (fewer false deltas)
     list_recent_search_ids() memoized with the same 120s window.
   All env-overridable.

3) miniapp/backend/app/services/nhentai_cache.py — dedup gallery walk
   capped: DEDUP_SCAN_MAX_ROWS (default 5000; <=0 restores old unbounded
   behaviour).

FILES IN THIS ZIP
-----------------
  miniapp/backend/app/routes/suggestions.py
  miniapp/backend/app/services/nhentai_cache.py
  Bot2Fetcher/app/turso_store.py
  GUIDE_APPEND.txt

DEPLOY
------
1) Drag-and-drop zip onto repo root; commit + push.
2) Redeploy Bot 0 (miniapp backend) — /similar now returns disabled by
   default. No env needed unless you later set SIMILAR_ENABLED=1.
3) Bot2Fetcher stays SUSPENDED until the Turso dashboard shows the read
   rate back under control; when you resume it, these tunables apply
   automatically.

VERIFY
------
  * Bot 0 log: /similar requests return {"error":"similar-disabled"} and
    NO "similar_sql" scan follows.
  * Turso dashboard: reads/min drops sharply within an hour of Bot 0
    redeploy (Bot 2 already suspended).
  * python3 -m py_compile on all 3 files: OK (done in sandbox).

HONEST NOTE
-----------
Sandbox instability this session meant validation was compile + source-
placement checks (gate placed before the scan call in the handler), not a
live HTTP test of the endpoint. If the mini-app's Similar section errors
instead of showing empty, set SIMILAR_ENABLED=1 to roll back instantly.
================================================================================
