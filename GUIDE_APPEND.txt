v12.51 — Cache bust, similar-to-this fix, admin/profile shim, telegram error handler, turso_schema fallback
Ship date: 2026-08-26

Files:
  miniapp/backend/main.py                    — turso_schema fallback, /js no-cache middleware
  miniapp/backend/app/db.py                  — is_admin_user shim
  miniapp/backend/app/services/__init__.py   — turso_schema re-export
  miniapp/frontend/index.html                — all JS refs bumped to ?v=12.51
  miniapp/frontend/js/plugins/detail-sheet.js — build marker + mountSimilarRow guard
  admin_bot.py                               — global telegram error handler

Why this fixes "Similar to this" not showing:
  - backend suggestions route was mounted and healthy (log: Mounted route module: suggestions)
  - frontend old bundle was cached; no /api/gallery/<id>/suggestions requests ever fired
  - index.html ?v=12.51 forces the new detail-sheet.js to load
  - /js/ now gets no-cache headers so future ships land instantly

Deploy order:
  1. Redeploy miniapp backend (main.py + db.py + services/__init__.py)
  2. Redeploy admin bot (admin_bot.py)
  3. Hard-refresh Telegram Mini App once on phone — new bundle downloads automatically
