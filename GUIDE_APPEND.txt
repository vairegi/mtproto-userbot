GUIDE_APPEND — v12.69 (2026-09-05) — Send NHENTAI_API_KEY on direct calls
WHAT: v12.68 backfill logic was correct but every attempt still failed
because scraper_bridge.py's three direct httpx.get() sites (/search and
two /galleries/<id>) sent no Authorization header. Anonymous v2
responses omit media_id + images.pages, which is exactly what triggered
"no images for <gid> (media_id/pages missing)" 32/32 times in the live
log. hf_scraper.py and ScraperBot/hf_scraper_lite.py already send the
key; scraper_bridge silently didn't. This was ALSO corrupting every new
gallery cache row (write path is anonymous → media_id lost → pages_meta
empty at birth). Now: module-level _NHENTAI_API_KEY read once at
import, shared _nh_headers() helper adds Authorization: Key <token> when
the env var is set, all three call sites switched. Anonymous fallback
still works (no key → no Authorization header, same as before).
FILES: miniapp/backend/app/services/scraper_bridge.py, GUIDE.md,
GUIDE_APPEND.txt. (No frontend change, no new env vars.)
DEPLOY: drop on repo root, commit, push, redeploy Bot 0 only. Confirm
NHENTAI_API_KEY is already in Bot 0's Render env (Bot 1 uses same var).
VERIFY: on first Read of an old gallery, Render log shows
  📖 [PAGES BACKFILL] gid=… pages=N cached for Read (put=True)
  📝 [TURSO WRITE] Uploaded payload to Turso  key=gallery:<id>
The reader opens with progress "1 / N". "no images (media_id/pages
missing)" should only appear now for galleries genuinely removed
upstream, not on every attempt.
