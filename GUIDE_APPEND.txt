GUIDE_APPEND — v12.86 (2026-09-07) — queue-ledger reaper
WHAT: the Queue tab's Pending badge (41) was phantom backlog — 42 pending
ledger rows all pointed at gids already COMPLETED in the DB channel, and
nothing reconciled the ledger against the state machine. Bot 2 now runs
reap_queue_ledger() once per scan cycle: pending rows whose gid is
COMPLETED/PARTIAL flip to 'completed' (one batched $in read, max 300 rows
per cycle). Parked (12h both-failed) and never-claimed gids stay pending.
FILES: Bot2Fetcher/app/mongo_state.py, Bot2Fetcher/app/fetcher.py,
GUIDE.md, GUIDE_APPEND.txt.
DEPLOY: drop on repo root, commit, push, redeploy Bot 2 ONLY.
VERIFY: first scan cycle logs "🧹 ledger reaper: flipped N stale pending
row(s)"; the mini-app Queue tab Pending count drops to the true value;
📥 badges in Discover are Bot 1's detail-hydration backlog (unrelated —
those galleries hydrate as Bot 1's sweeper gets to them).
