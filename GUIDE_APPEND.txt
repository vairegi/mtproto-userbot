================================================================================
GUIDE_APPEND — v12.61 (2026-09-05) — post-v12.60 log-review fixes
================================================================================

TRIGGER: full review of all 3 Render logs + GitHub Actions syntax check
after the v12.60 Mongo-2 migration deploy.

CONFIRMED WORKING (from your logs — no action needed):
  * Bot 2: dual-write live — "✍️ DUAL-WRITE key=... turso=OK mongo2=OK"
    on all 43 completed galleries.
  * Bot 0: Mongo-first reads live — 34 "📖 MONGO2 HIT", ZERO misses;
    "/similar gid=678407 source=MONGO2 scored=15792 rows -> 8 cards"
    (the migration goal achieved: /similar back, free of Turso reads).
  * Bot 1: discovery counting live — "🆕 popular-today page N: 1 new
    galleries" lines present (v1.27 fix working).

4 FIXES IN THIS ZIP
-------------------
1) miniapp/backend/app/services/nhentai_cache.py — removed the dead
   `_t0 = _time.monotonic() if False else None` line that failed GitHub's
   F821 syntax check (CI red on every push). Runtime was unaffected.

2) miniapp/backend/main.py — /static mount is now UNCONDITIONAL with path
   fallbacks + loud boot log ("✅ /static mounted from ..." or a 🚨
   error). Root cause of the broken admin tab: the old
   `if FRONTEND_DIR.is_dir()` guard silently skipped the mount in your
   deploy layout, so /static/js/pages/admin.js fell through to the SPA
   catch-all and returned index.html (HTML) instead of JS → browser threw
   "Failed to fetch dynamically imported module". The file was never
   missing from the repo — the server was answering it wrong.

3) ScraperBot/app/cache.py — the mongo2_client import is now OUTSIDE the
   try/except, and a dual-write failure logs at ERROR ("🚨 mongo2
   dual-write FAILED — mirror drifting"). Your Bot 1 log showed writes
   with turso=True and ZERO dual-write lines AND zero warnings — meaning
   the vendored mongo2_client.py was silently absent in that deploy.
   Now a missing file is a loud ImportError at first write, impossible
   to miss. ACTION REQUIRED: redeploy ScraperBot AFTER merging this zip;
   if the boot log shows an ImportError, the file isn't reaching Render
   and you must re-upload the zip's ScraperBot/app/mongo2_client.py.

4) admin_bot.py — exclusive flock (/tmp/admin_bot.lock) before
   run_polling. Render's rolling deploy overlapped two admin_bot pollers
   on the same bot token -> "telegram.error.Conflict: terminated by other
   getUpdates request" loop in your Bot 0 log. Now the second poller
   exits(3) instantly and start.sh's supervisor retries cleanly.

FILES
-----
  miniapp/backend/app/services/nhentai_cache.py
  miniapp/backend/main.py
  ScraperBot/app/cache.py
  admin_bot.py
  GUIDE.md
  GUIDE_APPEND.txt

DEPLOY
------
1) Drop zip on repo root; commit + push. GitHub CI syntax check should go
   green.
2) Redeploy ALL THREE services (Bot 0 needs the /static fix; ScraperBot
   needs the dual-write visibility; the admin_bot flock lives on Bot 0's
   service too since admin_bot runs there).
3) Verify:
   - Bot 0 boot log: "✅ /static mounted from ..." then open the Mini App
     admin tab — it loads.
   - ScraperBot Render log after first sweep: "✍️ DUAL-WRITE key=...
     turso=OK mongo2=OK" lines appear. If you instead see an ImportError,
     the vendored ScraperBot/app/mongo2_client.py didn't reach Render —
     re-upload it.
   - Bot 0 log: no more "telegram.error.Conflict" spam.

TESTS (sandbox)
---------------
  py_compile: all 4 touched files OK.
  tests/test_v12_60_mongo2.py — 20/20 PASS (no regressions).
  ScraperBot/tests/test_english_only.py — PASS.
  The /static fix is verified by construction (candidates checked with
  is_dir() + (js) presence at boot) — the first Bot 0 boot log line
  "✅ /static mounted from ..." is the live acceptance test.
================================================================================
