GUIDE_APPEND — v12.78 (2026-09-06) — instant pickup for user downloads
WHAT: user-tapped Downloads waited up to RESCAN_SLEEP_S (300s default) for
Bot 2's next scan cycle even with both slots idle (#661490: ~5min queued,
22s to complete). The producer's idle sleep is now interruptible: it polls
the queue ledger every 5s (one find_one-sized read) and wakes the moment a
user row lands; the full rescan cadence is unchanged. Peek errors are
swallowed.
FILES: Bot2Fetcher/app/fetcher.py, GUIDE.md, GUIDE_APPEND.txt.
DEPLOY: drop on repo root, commit, push, redeploy Bot 2 ONLY.
VERIFY: queue a gallery from the mini app while Bot 2 logs "💤 queue
drained — idle-waiting"; within ~5s the log shows "⚡ user queue row
detected — waking producer early" then "🎯 slot N claimed <gid>".
