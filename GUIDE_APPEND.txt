GUIDE_APPEND — v12.75 (2026-09-06) — freeze follow-ups
WHAT: (1) CI F821 fixed — suggestions.py now imports os. (2) The
"⚡ [TURSO CACHE HIT]" lines after the freeze were STALE LOG STRINGS
(zero turso.io calls in Bot 0's log — grep-verified); the four log
constants now say MONGO when BOT0_TURSO_OFF=1. (3) Bot 2 Mongo-2 mirror
bugs fixed: 32MB sort-limit abort → allow_disk_use + Python fallback;
list_recent_search_ids returned 0 ids because Mongo-2 payloads are JSON
STRINGS — now decoded first; get_gallery_row tolerates double-encoding;
ensure_schema skips Turso DDL under the freeze. (4) /popupmsg stored
literal "on" (live Mongo: popup_message='on') — on/off now redirect to
the toggle, writes are read back and previewed in the reply. Recovery:
/popupmsg clear, then /popupmsg <real message>.
FILES: admin_bot.py, miniapp/backend/app/routes/suggestions.py,
miniapp/backend/app/services/scraper_bridge.py,
Bot2Fetcher/app/turso_store.py, GUIDE*.
DEPLOY: drop on repo root, commit, push, redeploy ALL THREE bots; CI
must be green. Users reopen the Mini App once.
VERIFY: logs say "⚡ [MONGO CACHE HIT]" (not Turso); Bot 2
"📖 [MONGO READ] list_gallery_ids — N>0" no sort errors; /popupmsg hii
→ "✅ Popup updated — 📄 Saved message: hii" and mini-app shows "hii".
