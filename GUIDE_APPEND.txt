GUIDE_APPEND — v12.92 (2026-09-15) — /cleanrequest: unstick the queue badge + Bot 2 flip hardening
WHAT: The mini-app queue badge was pinned (e.g. 12 for days) by zombie queue
rows — Bot 2 had completed the downloads (galleries=COMPLETED) but the
best-effort queue-row flip missed pre-v12.88 rows that carry no gallery_id
field (URL-regex match silently failed). Now: (A) admin command
/cleanrequest (preview) + /cleanrequest confirm (apply) reconciles the queue
ledger against galleries truth — completed galleries get their queue rows
marked done (delivered="cleanup", no DM floods), rows >7d old with no
gallery activity in 24h get cancelled, genuinely active rows are untouched;
(B) Bot 2's mark_queue_status also matches by url_hash (sha256 of the
normalised URL) so legacy rows flip going forward; (C) /help now lists the
everyday commands hidden since v11.6 (/fetch /search /queue /status /token)
plus /cleanrequest.
FILES: admin_bot.py, Bot2Fetcher/app/mongo_state.py, GUIDE.md,
GUIDE_APPEND.txt.
DELETE: none.
DEPLOY: drop on repo root, commit, push, redeploy Bot 0 AND Bot 2. Bot 1
untouched. No new env vars.
VERIFY: in Bot 0's admin chat, /cleanrequest shows the preview counts;
/cleanrequest confirm replies "marked done (were completed): N" and the
mini-app badge drops immediately (queue pending+processing count falls).
New downloads complete with the badge returning to 0 without manual cleanup.
