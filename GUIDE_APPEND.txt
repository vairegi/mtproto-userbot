GUIDE_APPEND — v12.66 (2026-09-05) — In-app Read reader + worker removal
WHAT: Read (📖) button on the detail sheet opens plugins/reader.js:
webtoon vertical scroll (1-3 on open, +2/scroll step, ±10 mounted window,
2-ahead preload). Page images hotlink i1-i4.nhentai.net — zero server cost.
Backend: gallery detail carries pages_meta ([{n,path,w,h}]); contract
extended additively in all 3 vendored normalize.py copies; old rows
backfill lazily on first open. worker.py deleted; start.sh unsupervised.
FILES: plugins/reader.js (new), plugins/card-actions.js,
services/scraper_bridge.py, common|ScraperBot|Bot2Fetcher
turso_cache/normalize.py, start.sh, (worker.py deleted), GUIDE*.
DEPLOY: drop on repo root, commit, push, redeploy Bot 0 only; reopen the
Mini App fully once. VERIFY: Read button beside Share; reader opens with
sized shimmer, progress "n / total"; no worker boot lines; CI green.
