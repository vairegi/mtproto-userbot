================================================================================
GUIDE_APPEND — v1.23.2 (Bot2Fetcher: drop progress bar from log-channel post)
Shipped: 2026-08-29
================================================================================

WHAT CHANGED (one file): Bot2Fetcher/app/dashboard.py

Removed the "📊 Progress: `[███████░░░] N%`" line from the dashboard
message that Bot2Fetcher edits in the log channel. All other counters
(Turso cache, In DB channel, Failed-before, Remaining, per-slot state,
etc.) are unchanged — only the visual bar and its percentage disappear.
_progress_bar() itself is left in place so any future re-use is trivial.

FILES IN THIS ZIP
   Bot2Fetcher/app/dashboard.py   PATCH
   GUIDE.md / GUIDE_APPEND.txt    DOCS

DEPLOY STEPS
1. Drop onto the repo, commit, push → Bot2Fetcher redeploys on Render.
2. Within ~30s the pinned dashboard message re-renders without the bar.
================================================================================
