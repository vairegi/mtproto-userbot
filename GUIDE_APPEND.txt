GUIDE_APPEND — v12.82 (2026-09-07) — open-ended userbot slots
WHAT: STRING_SESSION_3 (and _4, _5, ...) now supported without a code
change — config.py discovers sessions until the first empty slot, capped
at 20 for safety. Backwards compatible: unset _3 -> 2 slots -> Bot 2
behaves exactly like today. Every downstream piece was already dynamic on
len(sessions) (slot workers, live dashboard, v12.81 5h digest) — new
slots show up in the log channel automatically. Priority order UNCHANGED:
user mini-app queue first, then recent scrapes, then bulk cache; all
slots pull in parallel from one shared queue.
FILES: Bot2Fetcher/app/config.py, GUIDE.md, GUIDE_APPEND.txt.
DEPLOY: drop on repo root, commit, push, redeploy Bot 2 ONLY.
SETUP for a 3rd userbot: (1) the new Telegram account must DM
@Gallery_DLBot once and join the DB channel; (2) generate its
StringSession; (3) set STRING_SESSION_3 in Render; (4) redeploy Bot 2.
VERIFY: boot log shows "📱 Sessions loaded: 3 userbot(s) — STRING_SESSION,
STRING_SESSION_2, STRING_SESSION_3"; live dashboard has three slot
sections; the next 5h digest lists three slot lines. Watch FloodWaits in
the digest — 3-4 slots is usually the sweet spot before @Gallery_DLBot
starts rate-limiting.
