================================================================================
GUIDE_APPEND — v1.23.0 re-ship (dashboard countdowns — the missing half)
Shipped: 2026-08-30
================================================================================

WHY THIS EXISTS
The v1.23.1 combined zip contained TWO changes: the mongo_client.py leak
fix and the channel_dashboard.py countdowns. Your repo has the leak fix
(verified: socketTimeoutMS=20000 + _STATE_INFLIGHT present) but the
dashboard file never made it in — so the log-channel message rendered
without the "· in Xh Ym" / "· due now" suffixes.

WHAT THIS ZIP CONTAINS (one file)
   ScraperBot/app/services/channel_dashboard.py — the countdown block:
   every sort/tag row gains "· in 2h 5m" (or "· due now") computed from
   the same list_sort_last:<sort> stamps + _sort_interval map that the
   v1.22.4 per-sort scheduler uses, so display always matches behavior:
       date → 2h · popular-today → 6h · popular-week → 12h
       popular → 24h · tag:<slug> → 24h
   Nothing else in the dashboard layout was touched (per operator
   request — countdowns only, no risky restyle).

   Plus GUIDE.md / GUIDE_APPEND.txt docs.

DEPLOY STEPS
1. Drop onto repo, commit, push → scraperbackup redeploys.
2. Within ~15s of boot the pinned message gains countdowns on every row.
3. Sorts never swept since v1.22.4 shipped show "due now" until their
   first run — correct behavior, not a bug.
================================================================================
