GUIDE_APPEND — v12.76b (2026-09-06) — workers panel stale-claim hotfix
WHAT: "Working now" cards were frozen on two old gids at 10% "Working…".
Cause: _workers sorted started_at ASC; Bot 2 refresh_claim bumps started_at
on every heartbeat, so live workers sort LAST and stale/abandoned PROCESSING
claims (no progress.* writes → empty stage → 10%) pinned the limit-4 window.
Fix: sort started_at DESC + skip docs whose progress.updated_at (or
started_at) is older than 300s. Backend-only change.
FILES: miniapp/backend/app/services/queue_bridge.py, GUIDE.md,
GUIDE_APPEND.txt.
DEPLOY: drop on repo root, commit, push, redeploy Bot 0 ONLY (Bot 2
untouched). No Mini App reload needed.
VERIFY: Queue tab "Working now" shows the gids Bot 2 is ACTUALLY logging
(🎯 slot N claimed <gid>) with real stage text and moving pct; the old
#510790/#665558 cards disappear on the next 8s poll.
