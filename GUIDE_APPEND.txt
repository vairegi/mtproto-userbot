GUIDE_APPEND — v12.84 (2026-09-07) — zombie reaper + repost throttle
WHAT: (1) Bot 2 idled while 40 mini-app queue rows sat pending: 3 dead
PROCESSING claims in galleries (zero-timestamp pre-v12.76 zombies + a
28h-expired lease) made claim_ex answer "busy" forever (87 busy-skips).
New reap_zombies() runs once per scan cycle: dead claims (no live lease)
become FAILED_RECOVERED — queue rows flip to failed (user can re-tap,
dedup_peek allows retry) and slots stop spinning. The legit 12h
both-failed park (#655846, future lease) is untouched. (2) Failed
log-channel reposts are now throttled by ATTEMPT (10min), fixing the
sendMessage-400-every-20s loop seen when the log bot lost admin rights.
FILES: Bot2Fetcher/app/mongo_state.py, Bot2Fetcher/app/fetcher.py,
Bot2Fetcher/app/dashboard.py, GUIDE.md, GUIDE_APPEND.txt.
DEPLOY: drop on repo root, commit, push, redeploy Bot 2 ONLY.
VERIFY: first scan cycle logs "🧹 reaped 3 zombie PROCESSING claim(s)";
mini-app Queue tab Pending drops from 40 as rows flip; dashboard Busy
stops climbing; "Remaining" reflects real work (fresh Bot-1 scrapes —
~197 uncached ids were waiting); no sendMessage-400 loops in Render logs.
