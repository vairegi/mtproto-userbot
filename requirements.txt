# v12.32: pinned to the 2026-08-20 successful build for reproducibility,
# PLUS pillow (the small-cover bug: cover_image_normalise needs Pillow to
# re-encode .webp/.png covers to JPEG; without it Telegram rejects the
# photo upload and falls back to a tiny document-style preview).
fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0
pymongo==4.7.2
python-dotenv==1.0.1
telethon==1.44.0
python-telegram-bot==22.8
pydantic==2.13.4
pillow==11.3.0
